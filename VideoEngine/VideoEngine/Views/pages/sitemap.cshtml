@inject IStringLocalizer<GeneralResource> GeneralResource
@{

    var _bModel = new List<BreadItem>();
    _bModel.Add(new BreadItem()
    {
        title = GeneralResource["_home"].Value,
        url = Config.GetUrl()
    });
    _bModel.Add(new BreadItem()
    {
        title = GeneralResource["_sitemap"].Value,
        isActive = true
    });
    var BreadCrumbViewModel = new BreadViewModel()
    {
        BreadItems = _bModel
    };
}
<partial name="~/Views/Shared/_BreadCrumb.cshtml" model="BreadCrumbViewModel" />


<div class="row">
    <div class="col-md-12">
        <h2 class="m-b-20">
            @GeneralResource["_sitemap"].Value
        </h2>
        <p>Design your sitemap structure according to your website link structure here...</p>
    </div>
</div>

